# SaveIt Environment Configuration
# Copy this file to .env.local and adjust values as needed

# =============================================================================
# DEV MODE
# =============================================================================
# Set to "true" to use mock data with localStorage (no database required)
# Set to "false" to use Supabase (requires credentials below)
NEXT_PUBLIC_DEV_MODE=true

# =============================================================================
# SUPABASE (only needed when NEXT_PUBLIC_DEV_MODE is false)
# =============================================================================
# Get these from your Supabase project dashboard
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# =============================================================================
# RECIPE EXTRACTION SERVICE (only needed when NEXT_PUBLIC_DEV_MODE is false)
# =============================================================================

# Redis (for job queue)
# Local development: redis://localhost:6379
# Production (Upstash): rediss://default:xxx@xxx.upstash.io:6379
REDIS_URL=redis://localhost:6379

# OpenAI API (for transcription and recipe analysis)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxx...

# Supabase Service Role Key (for backend operations)
# Find this in Supabase Dashboard > Settings > API > service_role key
# WARNING: Keep this secret! Never expose in frontend code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Recipe Extraction Mode
# Set to "recipe" for RecipeSave, or "general" for IG Downloader
EXTRACTION_MODE=recipe
